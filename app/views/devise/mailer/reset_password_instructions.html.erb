<div class="container my-5">
  <div class="content-wrapper p-4 bg-white rounded shadow-sm">
    <h2 class="mb-4 text-center">Hello, <%= @resource.email %>!</h2>
    <p class="lead text-center">Someone has requested a link to change your password. You can do this through the link below.</p>
    <p class="text-center">
      <%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token), class: "btn btn-primary btn-lg" %>
    </p>
    <p class="text-center">If you didn't request this, please ignore this email.</p>
    <p class="text-center">Your password won't change until you access the link above and create a new one.</p>
  </div>
</div>
